# 프로젝트 규칙

1. 요청한 기능 외에는 절대 수정하지 않는다.
2. 기존 함수명, 변수명, 이벤트를 유지한다.
3. 리팩토링은 요청받지 않는 이상 하지 않는다.
4. 수정 범위를 최소화한다 (Patch 방식).
5. 수정 후에는 변경한 코드 때문에 기존 기능이 깨질 가능성이 없는지 자체 검토한다.
6. 수정한 파일은 전체 파일로 제공하되, 어떤 부분을 수정했는지 요약한다.
7. 기존 UI와 동작을 유지한다.
8. 임시 코드나 테스트 코드는 남기지 않는다.

# 보안 규칙 (Supabase 등 외부 연동 작업 시 최우선 적용)

이 저장소는 **Public GitHub 저장소**다. 아래 규칙은 위 1~8번(수정 범위 최소화 등)보다 우선한다 — 보안 문제를 고치는 데 필요하다면 요청받지 않은 파일이라도 함께 손대고, 그 사실을 반드시 요약에 남긴다.

9. API 키·토큰·비밀번호는 코드에 절대 하드코딩하지 않는다. GAS는 `PropertiesService`(스크립트 속성), 그 외 환경은 env 변수에만 저장하고, 코드에는 값을 읽어오는 부분만 남긴다.
10. Supabase와 서버-서버(GAS 등 신뢰된 백엔드) 통신에는 `anon` key가 아니라 `service_role` key를 쓴다. `service_role` key는 절대 프론트엔드/클라이언트 코드나 공개 저장소에 넣지 않는다.
11. 새 Supabase 테이블을 만들면 생성 즉시 RLS를 켠다. `anon` role에게 `ALL`/`SELECT`를 조건 없이(`true`) 허용하는 정책은 만들지 않는다 — 기본은 전면 차단, 필요한 최소 범위만 허용.
12. 디버깅용으로 임시로 열어둔 RLS 정책(`temp_`, `test_` 등 이름)은 작업이 끝나면 바로 삭제한다. 방치하지 않는다.
13. 사번·전화번호 뒷자리 같은 "약한 값 조합"만으로 로그인/본인인증을 설계할 때는 계정 단위 잠금뿐 아니라 IP/전역 단위 요청 제한도 함께 걸고, 그 화면이 급여·연락처 등 민감정보를 보여주는지 먼저 확인한 뒤 사용자에게 남는 위험을 알린다.
14. 외부 웹훅(Slack 등) 수신 엔드포인트는 가능하면 서명 검증을 하고, 플랫폼 제약으로 불가능하면 그 사실과 남는 리스크를 사용자에게 명시적으로 알린다.
15. 새 외부 연동(Supabase, Slack, 결제, 문자 등)을 추가/수정한 뒤에는 매번 (a) 하드코딩된 시크릿 유무, (b) 관련 테이블 RLS·정책 상태, (c) 인증 없이 접근 가능한 엔드포인트 유무를 스스로 점검하고 결과를 요약해서 알린다.
